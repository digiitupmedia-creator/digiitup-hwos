import Link from 'next/link';
import { listKnowledgePackages } from '@/lib/knowledge-engine/read-packages';

export default async function KnowledgePackagesPage() {
  const packages = await listKnowledgePackages();
  return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-3"><div><h1 className="text-3xl font-bold">Knowledge packages</h1><p className="mt-2 text-slate-600">Imported, structured canonical knowledge available to future HWOS agents.</p></div><Link href="/knowledge/import" className="rounded-full bg-emerald-700 px-5 py-2 font-semibold text-white">Import knowledge</Link></div><div className="grid gap-4">{packages.map((item) => <article key={item.slug} className="rounded-2xl border border-emerald-100 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-semibold text-emerald-700">{item.documentId} · v{item.version}</p><h2 className="mt-1 text-xl font-bold">{item.title}</h2><p className="mt-1 text-sm text-slate-500">Imported {new Date(item.importedAt).toLocaleString()}</p></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-sm text-emerald-800">{item.status}</span></div><div className="mt-4 flex flex-wrap gap-4 text-sm text-slate-600"><span>{item.totalParts} parts</span><span>{item.totalChapters} chapters</span><span>{item.totalWordCount.toLocaleString()} words</span></div><Link href={`/knowledge/packages/${encodeURIComponent(item.slug)}`} className="mt-5 inline-block rounded-full border border-emerald-200 px-4 py-2 text-sm font-semibold text-emerald-900">Open package</Link></article>)}{packages.length === 0 && <p className="rounded-2xl bg-white p-6 text-slate-600">No knowledge packages imported yet.</p>}</div></div>;
}
