import { loadKnowledgeRegistry } from '@/lib/registry';
import { validateRegistries } from '@/lib/validation';

export default async function KnowledgeRegistryPage() {
  const [registry, validation] = await Promise.all([loadKnowledgeRegistry(), validateRegistries()]);
  return <div className="space-y-6"><div><p className="text-sm font-semibold uppercase tracking-wider text-emerald-700">Registry v{registry.version}</p><h1 className="mt-2 text-3xl font-bold">Knowledge registry</h1><p className="mt-2 text-slate-600">Canonical knowledge and department documents available to future agents.</p></div>{!validation.valid && <div className="rounded-2xl bg-red-50 p-5 text-red-800"><p className="font-bold">Registry validation errors</p><ul className="mt-2 list-disc pl-5">{validation.errors.map((error) => <li key={error}>{error}</li>)}</ul></div>}<div className="grid gap-4">{registry.documents.map((document) => <article key={document.id} className="rounded-2xl border border-emerald-100 bg-white p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-sm font-semibold text-emerald-700">{document.id} · {document.category}</p><h2 className="mt-1 text-xl font-bold">{document.title}</h2></div><span className="rounded-full bg-emerald-50 px-3 py-1 text-sm text-emerald-800">v{document.version} · {document.status}</span></div><p className="mt-3 text-slate-600">{document.description}</p><dl className="mt-4 grid gap-2 text-sm md:grid-cols-2"><div><dt className="font-semibold">File</dt><dd className="text-slate-600">{document.filePath}</dd></div><div><dt className="font-semibold">Owner</dt><dd className="text-slate-600">{document.ownedBy}</dd></div><div><dt className="font-semibold">Dependencies</dt><dd className="text-slate-600">{document.dependencies.join(', ') || 'None'}</dd></div><div><dt className="font-semibold">Immutable</dt><dd className="text-slate-600">{document.immutable ? 'Yes' : 'No'}</dd></div></dl></article>)}</div></div>;
}
